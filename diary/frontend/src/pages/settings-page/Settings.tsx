import MainContainer from "@/components/main-container/MainContainer.tsx";

const Settings = () => {
    return (
        <MainContainer>
            <div>
                Settings
            </div>
        </MainContainer>
    );
};

export default Settings;